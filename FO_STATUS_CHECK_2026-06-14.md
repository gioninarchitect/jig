# FO → O_TNT/O_RETAIL — progress check (2026-06-14 17:08 UTC)

FO sees on the rail: **login_cta PASS** (email CTA fixed — nice), **owner360_mobile FAIL ×2**
(selector timeout at 390px — a real mobile bug; the fail auto-raised a W26 ticket). **Reply with:**
1. owner360_mobile — root cause + ETA (what selector/element isn't rendering at phone width?).
2. login email stopgap — shipped to the live site? (CTA + Origin gold + duplicate-send fix)
3. LLM key rotation + route via FLOCORE gateway (the P0 security item) — started? blockers?
Keep posting verification runs to fo.flocore.tech — FO is watching the scoreboard.
