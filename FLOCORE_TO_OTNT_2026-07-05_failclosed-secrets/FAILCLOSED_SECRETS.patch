--- a/src/server/index.ts
+++ b/src/server/index.ts
@@ -16,6 +16,7 @@
  */
 
 import 'dotenv/config';
+import './secrets'; // fail-closed secret validation — aborts boot in prod if a secret is unset/default
 import express from 'express';
 import cors from 'cors';
 import path from 'path';
--- a/src/server/auth.ts
+++ b/src/server/auth.ts
@@ -10,12 +10,13 @@
 import jwt from 'jsonwebtoken';
 import { OTP_CONFIG } from '../world-model/types';
 import * as db from './db';
+import { JWT_SECRET, BYPASS_PIN } from './secrets';
 
 // ─────────────────────────────────────────────────────────────
 // CONFIG
 // ─────────────────────────────────────────────────────────────
 
-const JWT_SECRET = process.env.JWT_SECRET || 'puregro-dev-secret-change-in-production';
+// JWT_SECRET and BYPASS_PIN are loaded fail-closed from ./secrets (env-required in production).
 const JWT_EXPIRY = '7d';
 
 export const transporter = nodemailer.createTransport({
@@ -166,11 +167,10 @@
   userAgent?: string,
   ipAddress?: string,
 ): Promise<AuthResult> {
-  // Bypass PIN for demo/emergency access
-  const BYPASS_PIN = process.env.BYPASS_PIN || '830101';
-  console.log(`[AUTH] Verify attempt: email=${email} code=${otpCode} bypass=${otpCode === BYPASS_PIN}`);
+  // Bypass PIN for demo/emergency access — DEV-ONLY (null in production, see ./secrets).
+  console.log(`[AUTH] Verify attempt: email=${email} bypass=${!!BYPASS_PIN && otpCode === BYPASS_PIN}`);
 
-  if (otpCode !== BYPASS_PIN) {
+  if (!BYPASS_PIN || otpCode !== BYPASS_PIN) {
     // Check OTP normally
     const result = await db.verifyOtp(email, otpCode);
     console.log(`[AUTH] OTP check result: valid=${result.valid}`);
--- a/src/server/routes/pharmacy-core.routes.ts
+++ b/src/server/routes/pharmacy-core.routes.ts
@@ -1,9 +1,9 @@
 import { Router, Response, NextFunction } from 'express';
 import { requireAuth, requireAdmin, AuthenticatedRequest } from '../middleware';
 import * as pharmacy from '../pharmacy-core.service';
+import { INTERNAL_API_KEY } from '../secrets';
 
 const router = Router();
-const INTERNAL_API_KEY = process.env.INTERNAL_API_KEY || 'origin_internal_2026';
 
 function actor(req: AuthenticatedRequest): { actorType: 'user' | 'internal'; actorId?: string; ipAddress?: string } {
   if (req.headers['x-internal-key'] === INTERNAL_API_KEY) {
--- a/src/server/routes/products.routes.ts
+++ b/src/server/routes/products.routes.ts
@@ -11,11 +11,11 @@
 
 import { Router, Response, NextFunction } from 'express';
 import { optionalAuth, AuthenticatedRequest } from '../middleware';
+import { INTERNAL_API_KEY } from '../secrets';
 
 const router = Router();
 
 const POS_API_URL = (process.env.POS_API_URL || 'http://127.0.0.1:3004').replace(/\/$/, '');
-const INTERNAL_API_KEY = process.env.INTERNAL_API_KEY || 'puregro_internal_2026';
 
 // ── Shape Mapper: JIGPOS Product → B2B ProductData ──────
 
