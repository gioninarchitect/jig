# Session Recap — 2026-06-06

A very large day: live-POS firefighting → network onboarding → brand kit → a full AI operating-system architecture → and a **shipped Phase-1 build**. All grounded, nothing faked.

---

## 1. Live POS (Potchefstroom) — fixes shipped & verified
- **Root-caused & fixed "added products don't show":** the till is **branch-scoped** — products only appear with a `branchinventories` row. `createProduct` now auto-creates it; backfilled 4 orphans. (Owner account is role `branch_assistant`, PIN 480627.)
- **The `/pos/` URL trap** documented: `/pos/<static>` serves the marketing index; real POS is at ROOT paths. SW bumped to **v58**.
- **Reports button** on the till + **Email Takings Report to stakeholders** (recipient modal, persisted per branch).
- **Float fix:** `pos-shifts.js` defaulted to R500 → now R0; corrected the live session.
- **Takings cleaned:** excluded pre-launch test sales (R6,645 → true R2,176); reconciled card to **Capitec batch settlements** (3 Jun + 4–5 Jun tie to the cent; 1–2 Jun pending Ray).
- **Day-end incident:** close succeeded silently → Ray rang a sale after close → re-opened + closed clean (R40). Real gaps logged **#50**.

## 2. Network expansion — outlet onboarding (LIVE)
- `origin-onboarding.html` (admin-gated wizard, doc gate: SAPC/RP/CIPC/banking/premises; cannabis authority = ILCO's) + `origin-outlets.html` (review → **Verify & activate** = seeds the network catalogue @ 0 stock, switches the branch live). Backend `onboarding.controller` (submit/pending/approve/reject).
- First outlet queued: **Röscher Pharmacy, Kroonstad** (pending). Pending RP can't log in (`verify-pin` rejects inactive).

## 3. Brand kit
- **Design System**, **Origin + TnT-ZA Icon Series** (no emoji), **Origin Botanica** range proposal — all HTML + PDF. Lockup = **ORIGIN / by ILCO Farming**. Weekly **owner status report** (HTML+PDF).

## 4. Strategy & architecture (designed, grounded)
- **Role-activation gap analysis** (`tnt-za/role-activation-gap-analysis.html`) — per-role readiness; HoC = **GroOS** (separate app); 3 critical blockers (daily-check backend, QA-decision endpoint, S21 dispensing).
- **Academy LMS** plan (`tnt-za/docs/academy-build-plan.md`) — native, don't fork udemy-clones, study Lattice MVP.
- **Bug/idea assistant** spec (`tnt-za/docs/bug-assistant-spec.md`) + reusable **`in-app-feedback-assistant` skill**.
- **Operations Driver** design (`tnt-za/docs/operations-driver-design.md`) + one-pager (HTML+PDF) — Sense→Evaluate→Act→Surface over the ~80% that already exists.
- **Tenant distillation pipeline** (`tnt-za/docs/tenant-distillation-pipeline-design.md`) — vertical → roles → **micro-model** → drives dashboard+chat → **Sentinel governor**; compliance firewall; resilience/future-proof/**self-learning**; the **human remediation loop** (SOP violation → CAPA → targeted training → competency). Aligned to FLOCORE's canonical docs, not forked.

## 5. FLOCORE integration
- Replies: `FLOCORE_INTEGRATION_RESPONSE.md` (both repos) · `FLOCORE_ORCHESTRATOR_RECOMMENDATION.md` · `FLOCORE_AI_ARCHITECTURE_FEEDBACK.md` · `tnt-za/FLOCORE_OPERATIONS_DRIVER_CONTEXT.md` (FLOCORE drives the actions from our `driver.item.created` events).
- **Module agents created:** `.claude/agents/O_RETAIL_AGENT.md` + `O_TNT_AGENT.md`.
- **Open owner decisions: D1 (auth model) · D2 (security sign-off owner).** Awaiting FLOCORE deploy brief.

## 6. ✅ SHIPPED — Operations Driver Phase 1 (real code, verified)
Built in `tnt-za/backend` + `frontend` (per `docs/superpowers/plans/2026-06-06-operations-driver-phase1.md`):
- `DriverItem` model (`prisma db push` — project uses db push, NOT migrations).
- `driver.service.ts` rules engine → **18 drivers from real seeded data, idempotent** (verify-driver.ts PASS: 15 KPI · 2 anomaly · 1 inference).
- `driver.scheduler.ts` heartbeat (boot + 15m tick + event listeners) — verified in boot logs.
- Read-only API `/api/driver/queue(/me)` — mounted, auth-gated (401).
- `ActionQueueWidget.tsx` on the dashboard (type-clean, matches existing widget pattern).
- Emits `driver.item.created` (FLOCORE drives the action). 5 field-name corrections (from the build agent's read-only pass) baked in.
- **Caveats:** frontend not yet eyeballed live (needs Vite + login); **NOT committed** (per "commit only when asked").

## 7. New standing rules (saved to memory)
- Branded in-page dialogs only (never native) · **no over-engineering = no over-scoping** · always full URLs · **exhaustive scenario coverage** (surface every edge case; build only confirmed) · use `shipping-verified-frontend` / `verified-delivery` / `html-to-pdf` skills.
- Project memories: pos_branch_inventory · origin_nginx_pos_paths · roles_groos_academy · flocore_platform · flocore_micromodel_sentinel.

## 8. Top of next session
1. **Operations Driver Phase 1** — eyeball the dashboard card live + **commit**.
2. **Röscher Pharmacy** — onboard live → but do the **gap-A fix first** (`/online/` branch regex, #59).
3. **#50** day-end hardening.
4. FLOCORE **D1/D2** decisions + the formal module-manifest doc (#55).
5. Capitec CSV sample (#43) · farm-team training (#51).

Everything is in the design docs + memory + tasks #50–#59. Whoever (or whichever module agent) resumes has the full thread.
