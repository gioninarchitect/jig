# FLOCORE (FO) → ILCO agent cluster — NOTICE: cannabis go-live is GO for 18/06

**Date:** 2026-06-17 · **From:** FO (super admin: Floris) · **To:** O_TNT_AGENT · GROWOS_AGENT

---

## GO — rails verified
The 18/06 forward production genesis (first real cloning run) is **GO**. FLOCORE rails verified green:
Sentinel **15/15** · role grounding **34** (origin→ilco aliased) · CFS **66.2/B** · batch-start **gate live (409)** ·
schedule generator **14 stages / 117 days** · HOC role-chat **grounded**. Custody chain intact, untouched.

## Your run sheet (agent-assigned — read it)
**`FLOCORE_ILCO_GOLIVE_RUNSHEET_2026-06-18.md`** (in this handoff folder). In short:
- **O_TNT_AGENT (lead):** facility build → mothers registered as **CFS** + dual sign-off → cloning job from a
  **registered** mother → author the clone SOP green. (Loraine, Edgar, Lou, QA/Jeanette act in tnt-za.)
- **GROWOS_AGENT:** Lou (HOC) builds the **grow calendar** tenant-side (template: `POST /growos/cycle/schedule`).
- **O_RETAIL_AGENT:** not involved (retail is separate).

## Hold the line on these
- **CFS baseline ≠ forward genesis.** Existing mothers = `basis:"cfs"` + real `effective_date` (recorded now).
  The 18/06 clones = the forward genesis, signed **on the day**, real IDs (no `TEST-`).
- **HOC owns the calendar** (GrowOS). FO owns SOPs/custody/gate — not the calendar.
- **Recommend-only + dual sign-off** on regulated steps (incl QA). Append-only audit. Per-customer silo.

## On the day
**Ping FO when you start step 2 (mother registration).** FO watches the custody registrations → batch start →
audit trail **live** and confirms the genesis clean at step 6 (`/growos/batches` · `/audit/events` · `/cfs/indicator`).

## Comms
Route everything through **Floris (super admin)** — do not brief owners/clients/operators directly.

— FO
